# EXAMPLE-CORP 后台项目 — 背景笔记

> 虚构示范。**不是**真实项目，所有 URL / IP / 端口都是假的。

## 项目概览

| Item | Value |
|------|-------|
| 项目代号 | example-corp-portal |
| 域名 | portal.example.com |
| 仓库 | https://github.com/example-corp/portal |
| 技术栈 | Node.js 22 + TypeScript 5 + PostgreSQL 16 |
| Dev DB | 192.0.2.10:5432/example_dev（**RFC 5737 文档 IP**） |
| Prod DB | 198.51.100.10:5432/example_prod |
| 部署 | Docker Compose，host example-prod-01.example.internal |

## 关键模块

- `auth/` — 登录、JWT、refresh token
- `billing/` — 订单、发票、支付回调
- `notify/` — 邮件 / 站内信 / Webhook

## 关键联系人

- Tech Lead: alice@example.com
- PM: bob@example.com
- Ops: ops@example.com

## 重要决策记录

- 2026-05-01: 决定用 PostgreSQL 16，不用 MySQL（团队更熟 PG 的 JSONB）
- 2026-05-15: 引入 pgvector 做 embedding 搜索
- 2026-06-01: 上 Grafana 监控，Prometheus + Loki

## 陷阱

- 部署时 `docker-compose pull` 偶尔卡住，绕开：手动 `docker pull` 各 image
- 改 `.env` 后必须 `docker-compose up -d`（不是 `restart`，restart 不读 env）
